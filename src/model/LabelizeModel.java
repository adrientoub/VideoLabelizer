package model;

import framework.Application;
import framework.Model;

/**
 * Created by Adrien on 10/10/2016.
 */
public class LabelizeModel extends Model {
    /**
     * Initialize a new {@link Model} instance for the specified
     * {@link Application}.
     *
     * @param application The {@link Application} that the {@link Model} is
     *                    associated with.
     */
    public LabelizeModel(Application application) {
        super(application);
    }
}
